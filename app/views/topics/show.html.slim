#topic-detail.container-box
  .topic-header.p10
    = avatar_tag(@topic.user, name: false, img_class: "pull-right")
    .main
      h3 = @topic.title
      .meta
        span.time = @topic.created_at
        = link_to @topic.user.username
  .topic-content.p10 = @topic.body_html
  .topic-footer
    - if user_signed_in?
      - if stared?
        = link_to font_icon("star"), unstar_topic_path(@topic), id: "unstar", method: :patch
      - else
        = link_to font_icon("star-empty"), star_topic_path(@topic), id: "star", method: :patch

      - if can? :destroy, @topic
        = link_to font_icon("trash"), topic_path(@topic), id: "delete", method: :delete

- if @topic.replies.empty?
    #no-reply.text-center.muted  there is no reply currently
- else
    #replies.container-box
        .replies-header = pluralize(@topic.replies_count, "reply")
        = render @replies

= will_paginate @replies

#reply.container-box.p10
  -if user_signed_in?
    = render 'replies/form'
  -else
    p.text-center = link_to "sign in to reply", new_user_session_path
