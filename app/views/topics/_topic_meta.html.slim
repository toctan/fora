ul.topic__meta.zeta.fade.nav
  li.topic__meta-item By #{username_tag topic.username}
  li.topic__meta-item = timestamp(topic.created_at)
  li.topic__meta-item #{semantic_icon('heart')}#{topic.likes_count} Likes
  li.topic__meta-item #{topic.hits} hits
